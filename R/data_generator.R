#' Function to generate synthetic data
#'
#' @param mean
#' @param adj
#' @param n_samples
#' @param n_rois
#' @param n_volumes

rcm_data_generator <- function(mean      = rep(0, 10),
                               adj       = diag(1, 10),

                               n_samples = 10,
                               n_rois    = 10,
                               n_volumes = 50) {


  return(NULL)
}
